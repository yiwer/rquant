/target
/.rquant-cache

# IDE / 调试杂物
/.idea
rust_out.exe
rust_out.pdb
test_output.log
/report.json

# 实验临时数据
/tmps

# 桌面端留档（journal/回测 runs,可再生）
/.rquant-desktop

# 头脑风暴可视化伴侣会话（mockup/事件,不入库）
/.superpowers

# 纸面盘可变状态(state/行情/信号/日志——冻结树副本与运行脚本在 deploy/ 入库)
/paper

# deep-history research data (reproducible via data/fetch_deep.cmd)
data/*.csv
!data/universe_20.csv
# per-stock fundamental CSVs (reproducible via scripts/fetch_fundamentals.py)
data/fundamentals/

# daily-selection eval run outputs (reproducible via scripts/daily_eval.py)
.daily_runs/

# iteration-harness machine ledger (reproducible; md ledger is the human source of truth)
.iter/

# intraday 15m bars + derived daily factors + pilot universe (reproducible via
# scripts/fetch_intraday.py + build_intraday_factors.py + build_intraday_universe.py)
data/intraday_15m/
data/intraday_factors/
data/universe_intraday.csv
data/universe_intraday_symbols.txt

# baostock 5yr backtest dataset (daily/15m bars + indicator features + sectors;
# reproducible via scripts/{build_universe_union,fetch_baostock,build_indicators,build_sectors}.py)
data/baostock/

# xueqiu 15m bars (reproducible via scripts/fetch_xueqiu_15m.py)
data/xueqiu/
# 掘金 gm 15m bars (reproducible via scripts/fetch_gm_realtime.py)
data/gm/
# login cookie / api token / session secret — SECRET, never commit
*.cookie
.xq_cookie
*.token
.gm_token
